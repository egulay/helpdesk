spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
  cloud:
    vault:
      enabled: false
springdoc:
  api-docs:
    enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false