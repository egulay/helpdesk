spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false


logging:
  level:
    org:
      springframework: OFF